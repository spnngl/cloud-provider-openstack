{{- if .Values.csi.plugin.nodePlugin.enable }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cinder-csi.name" . }}-cinder-node-sa
  namespace: {{ .Release.Namespace }}
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "cinder-csi.name" . }}-nodeplugin-role
rules:
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]

---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "cinder-csi.name" . }}-nodeplugin-binding
subjects:
  - kind: ServiceAccount
    name: {{ include "cinder-csi.name" . }}-cinder-node-sa
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ include "cinder-csi.name" . }}-nodeplugin-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}
